---
name: number
route: /api-number
menu: API reference
---

import { Code } from '../components/Code';
import { Comparison } from '../components/Comparison';
import { signature, example, before, after } from '../code/api-number';

# number

Adds description for **Number** field in `DataCaster` instance.

> This method is chainable.

## Signature

<Code>
    {signature}
</Code>

> `options` is an object that can be omitted. To get more about options see [Fields](/basics#fields) section

## Example

<Code>
    {example}
</Code>

<Comparison
    before={before}
    after={after}
/>
