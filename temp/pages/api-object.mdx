---
name: object
route: /api-object
menu: API reference
---

import { Code } from '../components/Code';
import { Comparison } from '../components/Comparison';
import { signature, example, before, after } from '../code/api-object';

# object

Adds description for **Object** field in `DataCaster` instance.

> This method is chainable.

## Signature

<Code>
    {signature}
</Code>

> `options` is an object that can be omitted. To get more about options see [Fields](/basics#fields) section

## Example

<Code>
    {example}
</Code>

<Comparison
    before={before}
    after={after}
/>
